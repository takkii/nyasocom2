<div class="right">
  <%= link_to 'トップページ', root_path, class: "btn btn-primary" %> <%= link_to '新規追加', new_book_path, class: "btn btn-primary" %> <%= link_to 'ブログ', blogs_path, class: "btn btn-primary" %> <%= link_to "CSV (SJIS)", books_path(format: :csv), class: "btn btn-primary" %>
  <% if user_signed_in? %><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-primary" %>
  <% end %>
</div>

<br/>
<h2>更級日記 ~ nyasocom2編 ~ </h2>
<br/>

<% @blogs.each do |blog| %>
  <p><h2><%= blog.days %></h2></p>
  <p><h3><%= blog.title %></h3></p>
  <p><%= markdown(blog.memo).html_safe %></p>
<% end %>

<br/>

<%= link_to 'ブログへ戻る', blogs_path %>
